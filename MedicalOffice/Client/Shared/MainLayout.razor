@inherits LayoutComponentBase
@inject NavigationManager NavigationManager

<MudRTLProvider>

    <MudThemeProvider />
    <MudDialogProvider />
    <MudSnackbarProvider />

    <MudLayout>
        <NavMenu />
        <MudMainContent Class="pt-16 px-16 mt-2" MaxWidth="MaxWidth.False">
            @Body
        </MudMainContent>
    </MudLayout>


</MudRTLProvider>

@* @code {

    [CascadingParameter]
    protected Task<AuthenticationState> AuthStat { get; set; }

    protected async override Task OnInitializedAsync()
    {
        base.OnInitialized();
        var user = (await AuthStat).User;
        if (!user.Identity.IsAuthenticated)
        {
            NavigationManager.NavigateTo($"admin/login?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}");
        }
    }
} *@



